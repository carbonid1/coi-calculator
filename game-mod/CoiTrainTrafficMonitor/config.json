{
  "stuck_after_cycles": {
    "default": 1,
    "min": 1,
    "max": 12,
    "is_integer": true,
    "description": "Train considered stuck after this many consecutive production cycles waiting for track clearance."
  },
  "pause_on_red_alert": {
    "default": false,
    "description": "Pause once when the Train Traffic Monitor red alert begins. Other game alerts are not affected, and the game never resumes automatically."
  }
}
