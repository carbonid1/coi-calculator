interface:
  display_name: "Rebalance Farms"
  short_description: "Plan COI crop rotations against factory demand"
  default_prompt: "Use $rebalance-farms to propose crop rotations for factory demand while preserving synced farm state."
